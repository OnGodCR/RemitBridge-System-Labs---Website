node_modules/
dist/

# graphify knowledge-graph output; regenerate with `graphify . --code-only`
graphify-out/

# secrets — .env.example is the checked-in template
.env
.env.local

.DS_Store
*.local
.vite/

# holds the owner email; use set-owner.sql.example as the template
supabase/set-owner.sql
supabase/set-notify.sql

# World Bank RPW spreadsheet, 50 MB; scripts/rpw.mjs turns it into public/data/rpw
data/raw/
