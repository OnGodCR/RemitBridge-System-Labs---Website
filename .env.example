# Supabase project credentials (Dashboard → Project Settings → API).
# Copy to .env.local. Both values are safe in a browser bundle.
# Never put the service_role / secret key here — it bypasses row-level security.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
